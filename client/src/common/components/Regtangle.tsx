import { palette } from '@utils/palette';

const Regtangle = () => {
  return (
    <div
      css={{
        width: '12px',
        height: '12px',
        background: palette.point.blue,
      }}
    ></div>
  );
};

export default Regtangle;
