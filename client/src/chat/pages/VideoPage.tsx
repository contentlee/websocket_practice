import { PageLayout } from "@components";
import { VideoContainer } from "../containers";

const VideoPage = () => {
  return (
    <PageLayout>
      <VideoContainer />
    </PageLayout>
  );
};

export default VideoPage;
